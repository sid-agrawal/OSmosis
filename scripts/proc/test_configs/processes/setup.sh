#!/usr/bin/env bash
# setup.sh — Baseline: two plain hello processes.
# Outputs CONFIG=0 so conftest lets proc_model.py start, extract, and kill them.
# run_configs[0] = two basic hello processes.
echo "CONFIG=0"
